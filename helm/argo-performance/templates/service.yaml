apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-performance.fullname" . }}
  labels:
    {{- include "argo-performance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-performance.selectorLabels" . | nindent 4 }}
